<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end items-center gap-4 mb-5">
            <a href="/profile/upload" class="bg-yellow-500 rounded-md px-6 py-2 text-sm hover:bg-yellow-600">
                Upload Picture
            </a>

            <a href="/logout" class="bg-red-500 rounded-md px-6 py-2 text-sm hover:bg-red-600">
                Logout
            </a>
        </div>

        <div class="flex items-start gap-5">
            <div class="w-16 h-16 rounded-md overflow-hidden">
                <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>" alt="">
            </div>
            <h3 class="text-5xl mb-5">
                <span class="font-light">Hello</span>, <%= user && user.name ? user.name : 'User' %>
            </h3>
        </div>
        <h5 class="mb-5">You Can Create A New Post.</h5>


        <form action="/post" method="post">
            <textarea
                class="w-2/6 resize-none bg-transparent border-2 border-zinc-800 rounded-md p-2 outline-none block"
                placeholder="Whats on your mind?" name="content"></textarea>
            <input class="px-5 w-52 py-2 bg-blue-500 block  rounded-md mt-2 text-sm" type="submit"
                value="Create New Post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-500">Your Posts.</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-5/6 rounded-md p-5 mb-5 border-[1px] border-zinc-800">
                        <h4 class="text-blue-500 mb-2">@<%=user.username%>
                        </h4>
                        <p class="text-sm tracking-tighter">
                            <%=post.content%>
                        </p>
                        <small class="mt-5 inline-block">
                            <%= post.likes.length %>likes
                        </small>
                        <div class="btns flex gap-5 mt-1">
                            <a class="text-blue-500" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(currentUserId)===-1 ? "Like" : "Unlike" %>
                            </a>
                            <a class="text-zinc-600" href="/edit/<%= post._id %>">edit</a>
                        </div>
                    </div>
                    <% }); %>
            </div>

        </div>
    </div>

</body>

</html>